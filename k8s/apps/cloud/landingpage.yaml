apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: landingpage
spec:
  interval: 24h
  chart:
    spec:
      chart: ./appchart
      version: "x.x.x"
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 10m
  values:
    apps:
      - name: landingpage
<<<<<<< Updated upstream
        image: gitea.cocointhe.cloud/coco/landingpage:v0.3.1@sha256:58e0590ebeb0ce4c4c4eb1d07725bc030bc9d0cf3917440551b2654842065c1c
=======
        image: gitea.cocointhe.cloud/coco/landingpage:v0.3.1
>>>>>>> Stashed changes
        service: 5000
        ingress:
          public: true
          domain: cocointhe.cloud
        env:
          - name: LLM_API_URL
            value: http://192.168.1.129:8080/v1/chat/completions
        volumeMounts:
          - name: landingpage-data
            mountPath: /app/static
        volumes:
          - name: landingpage-data
            persistentVolumeClaim:
              claimName: landingpage-data
        pvc:
          - landingpage-data

---
# acess llamacpp host and prometheus
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-landingpage
spec:
  podSelector:
    matchLabels:
      app: landingpage
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 192.168.1.129/32
    - to:
      - namespaceSelector: {}
        podSelector:
          matchLabels:
            app.kubernetes.io/name: prometheus
