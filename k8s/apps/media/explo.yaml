apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: explo
spec:
  interval: 10m
  chart:
    spec:
      chart: ./appchart
      version: "x.x.x"
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 10m
  values:
    apps:
      - name: explo
        image: ghcr.io/lumepart/explo:v1.1.2@sha256:f22b9131501c68e68a2598b2e6ddceeda7ac547ab6ea1bb701b49f93f54b7f9d
        env:
          - name: EXECUTE_ON_START
            value: "true"
          - name: WEB_UI
            value: "true"
          - name: UI_USERNAME
            value: "testuser"
          - name: UI_PASSWORD
            value: "testpassword"
        volumeMounts:
          - name: jellyfin-music
            mountPath: /data
          - name: explo-data
            mountPath: /opt/explo/config
          - name: explo-config
            mountPath: /opt/explo/.env
            subPath: .env
        volumes:
          - name: jellyfin-music
            persistentVolumeClaim:
              claimName: jellyfin-music
          - name: explo-data
            persistentVolumeClaim:
              claimName: explo-data
          - name: explo-config
            secret:
              secretName: explo-config
        service: 7288
        ingress:
          domain: explo.cocointhe.cloud
        pvc:
          - explo-data
